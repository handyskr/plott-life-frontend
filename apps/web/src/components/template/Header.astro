---
const { showBackIcon, showCloseIcon, showHomeIcon, title } = Astro.props;

const hasDefaultSlot = !!Astro.slots.default;
---

<header class='sticky top-0 w-full z-10 flex justify-center min-h-[64px]'>
  <div class='flex max-w-[600px] w-full items-center bg-white'>
    {hasDefaultSlot ? (
      <slot />
    ) : (
      <div class='col flex items-center gap-2 min-w-[40px] justify-start'>
        {
          showBackIcon && (
            <button aria-label='뒤로가기'>
              <svg width='24' height='24' fill='none'>
                <path d='M15 18l-6-6 6-6' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round' />
              </svg>
            </button>
          )
        }
      </div>
      <div class='col flex-1 flex items-center justify-center text-black'>
        {title}
      </div>
      <div class='col flex items-center gap-2 min-w-[40px] justify-end'>
        {
          showCloseIcon && (
            <button aria-label='닫기'>
              <svg width='24' height='24' fill='none'>
                <path d='M6 6l12 12M6 18L18 6' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round' />
              </svg>
            </button>
          )
        }
        {
          showHomeIcon && (
            <button aria-label='홈'>
              <svg width='24' height='24' fill='none'>
                <path d='M6 6l12 12M6 18L18 6' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round' />
              </svg>
            </button>
          )
        }
    </div>
      </div>
    )}
</header>